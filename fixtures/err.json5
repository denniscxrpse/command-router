// Bad formatted JSON file, do NOT use this an example, and DO NOT parse it.
// This file is meant for error handling for HTTP POST requests.
{
  "cmd-router": {
    // Invalid schema version
    "schema-version": -1,
    "grammar": {
      // Invalid grammar map
      "gamemode": 5,
    },
    // Unknown key
    "unknown": "key"
  }
}