[cmd-router]
schema-version = 1

[cmd-router.grammar]
# — nesting + optional
advancement = "(grant|revoke) <target> ('*' | only <advancement> [<criterion>])"
# — single greedy arg, no branching at all
say = "<message...>"
